package inference

class InferenceChecker {
    val inferenceSets = mutableMapOf<String, Set<Inference>>()

    fun addInference(list: List<Inference>) {
        for (inference in list) {

        }
    }
}
