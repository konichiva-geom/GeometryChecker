PLAIN:
     В треугольниике ABC проведены высота BH, медиана BM, биссектриса BD.

     Докажите, D что лежит на отрезке HM.
DESCRIPTION:
    new A, new B, new C
    bisector(new D, ABC)
    mid_point(new M, AC)
    projection(new H, B, AC)
PROVE:
    D in segment HM
SOLUTION:
    circumscribed_circle(new omega, ABC)
    P = ray BD intersects omega // TODO we should check that:
    // 1. B in omega
    // 2. D inside omega. For that, need to make sure that D in AB, where A in omega, B in omega.
    angle_


th circumscribed_circle(new omega, ABC):
    return A in omega, B in omega, C in omega

th arcs_relation_from_angles(ABP, PBC, omega arc AP, omega arc PC):
    // check that arcs are on same circle
    omega = circle(arc AP)
    omega_2 = circle(arc PC)
    check(omega == omega_2)
    check(B in omega)
    return ABP / PBC = AP / PC